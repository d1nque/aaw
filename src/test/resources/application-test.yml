spring:
  application:
    name: aaw
  devtools:
    restart:
      enabled: false  # Вимкнути restart для швидкого запуску
    livereload:
      enabled: false  # Вимкнути LiveReload

# Test configuration - disable hardware features
features:
  gpio-buttons:
    enabled: false
  lrf:
    enabled: false
  camera:
    day-camera-enabled: false
    thermal-camera-enabled: false
  logger:
    enabled: false

